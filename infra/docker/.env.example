# Copy to .env and fill in values before running docker-compose
# This file is used by infra/docker/docker-compose.yml

# AI Providers
OPENAI_API_KEY=
HUGGINGFACE_TOKEN=

# Optional: Web3 provider & contract settings for backend
WEB3_PROVIDER=https://rpc.testnet.linea.xyz
CONTRACT_ADDRESS=

# Optional: IPFS external gateway override (frontend)
NEXT_PUBLIC_IPFS_GATEWAY=http://localhost:8080/ipfs

# Optional: Public API URL for frontend
NEXT_PUBLIC_API_URL=http://localhost:4000
